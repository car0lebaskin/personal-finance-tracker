'use client';

import { useEffect, useMemo, useState } from 'react';
import { useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import { cn } from '@/lib/utils';
import { AlertTriangle, ArrowUpRight, BarChart3, Coins, CreditCard, Home, Landmark, Lightbulb, Plus, ShieldCheck, Sparkles, Trophy, UserCircle, Wallet, Waves } from 'lucide-react';

type Account = { id: string; name: string; institution: string; type: string; balance: number; currency: string };
type Tab = 'accounts' | 'insights' | 'playground';
type Recommendation = { title: string; body: string; priority: 'High' | 'Medium' | 'Low'; icon: React.ElementType };

const liabilityTypes = ['loan', 'credit'];
const typeLabel: Record<string, string> = { checking: 'Checking', savings: 'Savings', cash: 'Cash', investment: 'Investment', crypto: 'Crypto', retirement: 'Retirement', property: 'Property', loan: 'Loan', credit: 'Credit' };
const typeIcon: Record<string, React.ElementType> = { checking: Landmark, savings: Landmark, cash: Wallet, investment: BarChart3, crypto: Coins, retirement: Trophy, property: Home, loan: CreditCard, credit: CreditCard };

function formatCurrency(value: number, compact = false) { return new Intl.NumberFormat('en-MY', { style: 'currency', currency: 'MYR', maximumFractionDigits: compact ? 1 : 0, notation: compact ? 'compact' : 'standard' }).format(value || 0); }
function getRecommendations(totals: { assets: number; liabilities: number; cash: number; investments: number; property: number; netWorth: number }): Recommendation[] {
  const assetBase = Math.max(totals.assets, 1); const cashPct = (totals.cash / assetBase) * 100; const investPct = (totals.investments / assetBase) * 100; const debtPct = (totals.liabilities / assetBase) * 100; const propertyPct = (totals.property / assetBase) * 100; const recs: Recommendation[] = [];
  if (totals.assets === 0) return [{ title: 'Build the base layer first', body: 'Add cash, EPF, investments, crypto, property, and loans before relying on allocation advice.', priority: 'High', icon: ShieldCheck }];
  if (cashPct > 35) recs.push({ title: 'Cash allocation looks heavy', body: `Cash is about ${cashPct.toFixed(0)}% of assets. Keep an emergency buffer, then consider reallocating surplus into EPF, broad funds, or investment accounts.`, priority: 'High', icon: Wallet });
  if (investPct < 25 && totals.netWorth > 0) recs.push({ title: 'Investment allocation is light', body: `Investments are about ${investPct.toFixed(0)}% of assets. Retirement progress may need a higher allocation outside idle cash.`, priority: 'Medium', icon: BarChart3 });
  if (debtPct > 40) recs.push({ title: 'Debt load needs monitoring', body: `Liabilities are about ${debtPct.toFixed(0)}% of assets. Prioritise expensive debt before increasing risk assets.`, priority: 'High', icon: AlertTriangle });
  if (propertyPct > 60) recs.push({ title: 'Net worth is property-heavy', body: `Property is about ${propertyPct.toFixed(0)}% of assets. Build liquid assets so your balance sheet is less locked up.`, priority: 'Medium', icon: Home });
  if (recs.length === 0) recs.push({ title: 'Allocation looks balanced', body: 'Your cash, investments, and liabilities are not showing obvious concentration issues.', priority: 'Low', icon: ShieldCheck });
  return recs.slice(0, 4);
}

export default function DashboardPage() {
  const router = useRouter(); const [accounts, setAccounts] = useState<Account[]>([]); const [loading, setLoading] = useState(true); const [tab, setTab] = useState<Tab>('accounts');
  useEffect(() => { async function loadAccounts() { const { data: sessionData } = await supabase.auth.getSession(); if (!sessionData.session) { router.push('/login'); return; } const { data, error } = await supabase.from('accounts').select('id, name, institution, type, balance, currency').order('created_at', { ascending: false }); if (!error && data) setAccounts(data as Account[]); setLoading(false); } loadAccounts(); }, [router]);
  const totals = useMemo(() => { const assets = accounts.filter((a) => !liabilityTypes.includes(a.type)).reduce((s, a) => s + Number(a.balance), 0); const liabilities = accounts.filter((a) => liabilityTypes.includes(a.type)).reduce((s, a) => s + Math.abs(Number(a.balance)), 0); const cash = accounts.filter((a) => ['checking', 'savings', 'cash'].includes(a.type)).reduce((s, a) => s + Number(a.balance), 0); const investments = accounts.filter((a) => ['investment', 'crypto', 'retirement'].includes(a.type)).reduce((s, a) => s + Number(a.balance), 0); const property = accounts.filter((a) => a.type === 'property').reduce((s, a) => s + Number(a.balance), 0); return { assets, liabilities, cash, investments, property, netWorth: assets - liabilities }; }, [accounts]);
  const recommendations = useMemo(() => getRecommendations(totals), [totals]); const startNetWorth = Math.max(totals.netWorth * 0.62, 0); const change = totals.netWorth - startNetWorth; const changePct = startNetWorth > 0 ? (change / startNetWorth) * 100 : 0; const projected = totals.netWorth + Math.max(change, totals.netWorth * 0.08);
  const allocation = [{ label: 'Cash', value: totals.cash }, { label: 'Investments', value: totals.investments }, { label: 'Property', value: totals.property }].filter((item) => item.value > 0);
  if (loading) return <main className="min-h-screen bg-[#080b08] flex items-center justify-center text-[#d8ded2]">Loading...</main>;
  return <main className="min-h-screen bg-[#080b08] text-[#f4f5ef]"><div className="mx-auto max-w-[720px] min-h-screen relative overflow-hidden pb-24"><div className="absolute inset-0 pointer-events-none bg-[radial-gradient(circle_at_50%_-10%,rgba(148,255,97,0.14),transparent_36%),linear-gradient(180deg,#1b2318_0%,#080b08_42%)]"/><div className="relative px-5 pt-7"><header className="flex items-center justify-between mb-7"><h1 className="text-3xl font-semibold tracking-tight">{tab === 'accounts' ? 'Worth it' : tab === 'insights' ? 'Insights' : 'Playground'}</h1><div className="flex items-center gap-3"><button className="rounded-full bg-gradient-to-r from-[#2f7dff] to-[#28d5c4] px-3 py-1.5 text-xs font-bold text-white">Get ✨</button><UserCircle className="h-10 w-10"/></div></header>{tab === 'accounts' && <section><p className="text-[#a8aca3] text-base mb-2">Net worth</p><div className="text-4xl font-light tracking-tight mb-3">{formatCurrency(totals.netWorth)}</div><div className="inline-flex items-center gap-2 rounded-lg bg-[#75efad]/20 px-3 py-1 text-[#75efad] font-mono text-sm mb-4"><span>▲</span>{changePct.toFixed(1)}%</div><div className="h-[180px] -mx-5 mb-5 border-b border-white/10 relative overflow-hidden"><svg viewBox="0 0 700 180" className="h-full w-full"><defs><linearGradient id="line" x1="0" x2="1"><stop offset="0%" stopColor="#35bdf5"/><stop offset="100%" stopColor="#69f0c2"/></linearGradient><linearGradient id="fill" x1="0" y1="0" y2="1"><stop offset="0%" stopColor="#69f0c2" stopOpacity="0.20"/><stop offset="100%" stopColor="#69f0c2" stopOpacity="0"/></linearGradient></defs>{[100,230,360,490,620].map((x) => <line key={x} x1={x} x2={x} y1="0" y2="180" stroke="rgba(255,255,255,0.10)"/>)}<path d="M0 130 C120 120,200 105,290 94 C390 78,480 58,700 54" fill="none" stroke="url(#line)" strokeWidth="4" strokeLinecap="round"/><path d="M0 130 C120 120,200 105,290 94 C390 78,480 58,700 54 L700 180 L0 180 Z" fill="url(#fill)"/></svg><div className="absolute bottom-2 left-0 right-0 flex justify-between px-5 text-[#8d9188] text-sm"><span>Jan</span><span>Feb</span><span>Mar</span><span>Apr</span><span>May</span><span>Jun</span></div></div><div className="flex gap-3 mb-4"><div className="flex-1 rounded-2xl border border-white/12 bg-white/[0.04] px-4 py-3 text-sm text-[#d8ded2]"><Wallet className="inline h-4 w-4 mr-2"/>{accounts.length} accounts</div><button className="rounded-2xl bg-[#4a573d] px-4 py-3 font-mono text-sm">Filter</button></div><div className="space-y-1">{accounts.map((account) => { const Icon = typeIcon[account.type] ?? Landmark; const debt = liabilityTypes.includes(account.type); return <div key={account.id} className="relative flex items-center justify-between rounded-2xl px-4 py-3"><div className={cn('absolute left-0 top-3 bottom-3 w-1.5 rounded-full', debt ? 'bg-[#c96f5d]' : account.type === 'retirement' ? 'bg-[#31b8d8]' : 'bg-[#a7ff4f]')}/><div className="pl-4 min-w-0"><p className="text-xl truncate">{account.name}</p><div className="mt-1 flex items-center gap-2 text-[#8d9188]"><span className="inline-flex items-center gap-1 rounded-full border border-white/10 bg-white/[0.04] px-2 py-0.5 text-xs"><Icon className="h-3 w-3"/>{typeLabel[account.type] ?? account.type}</span></div></div><p className="text-xl shrink-0">{formatCurrency(Math.abs(Number(account.balance)))}</p></div>})}</div></section>}{tab === 'insights' && <section className="space-y-5"><div><h2 className="text-3xl font-light">Big picture</h2><p className="text-[#a8aca3] text-base">Forecast and next milestone at a glance.</p></div><div className="rounded-[24px] bg-white/[0.05] border border-white/8 p-5"><div className="flex gap-4"><div className="w-12 h-12 rounded-2xl bg-white/[0.06] flex items-center justify-center"><Sparkles className="h-6 w-6 text-[#75efad]"/></div><div className="flex-1"><h3 className="text-2xl mb-1">Net Worth Forecast</h3><p className="text-[#d8ded2] text-base">Reaching <b>{formatCurrency(projected)}</b> by year-end.</p><div className="mt-5 space-y-2 font-mono text-sm"><div className="flex justify-between"><span className="text-[#a8aca3]">So far this year</span><span>{formatCurrency(change)} <b className="text-[#75efad]">▲ {changePct.toFixed(1)}%</b></span></div><div className="flex justify-between"><span className="text-[#a8aca3]">Projected gain</span><span>{formatCurrency(projected - totals.netWorth)}</span></div></div></div></div></div><div className="rounded-[24px] bg-white/[0.05] border border-white/8 p-5"><h3 className="text-2xl mb-1">Next Milestone</h3><p className="text-[#d8ded2] text-base">Next target: {formatCurrency(Math.ceil((totals.netWorth + 1) / 100000) * 100000)}</p></div><div><h2 className="text-3xl font-light">Momentum</h2><p className="text-[#a8aca3] text-base">Recent changes and liquid position.</p></div><div className="rounded-[24px] bg-white/[0.05] border border-white/8 p-5"><ArrowUpRight className="h-8 w-8 text-[#75efad] mb-3"/><h3 className="text-2xl">Monthly Progress</h3><p className="text-[#d8ded2] text-base">+{formatCurrency(change)} this period</p></div><div className="rounded-[24px] bg-white/[0.05] border border-white/8 p-5"><div className="flex items-center justify-between mb-4"><h3 className="text-2xl">AI Recommendations</h3><Lightbulb className="h-5 w-5 text-[#75efad]"/></div><div className="space-y-3">{recommendations.map((rec) => <div key={rec.title} className="rounded-2xl bg-black/20 border border-white/8 p-4"><div className="flex gap-3"><rec.icon className="h-5 w-5 text-[#75efad] shrink-0 mt-1"/><div><p className="font-semibold text-base">{rec.title}</p><p className="text-sm text-[#a8aca3] leading-relaxed">{rec.body}</p></div></div></div>)}</div></div></section>}{tab === 'playground' && <section className="space-y-5"><div className="flex justify-between"><div><p className="text-[#a8aca3] text-base">From</p><p className="text-3xl font-light">{formatCurrency(startNetWorth)}</p><p className="text-base">Jan 1, 2026</p></div><div className="text-right"><p className="text-[#a8aca3] text-base">To</p><p className="text-3xl font-light">{formatCurrency(totals.netWorth)}</p><p className="text-base">Today</p></div></div><div className="h-[260px] -mx-5 border-y border-white/10 relative"><svg viewBox="0 0 700 260" className="w-full h-full"><path d="M30 210 C150 180,230 115,360 75 C470 45,540 30,650 34" fill="none" stroke="#69f0c2" strokeWidth="4" strokeLinecap="round"/><circle cx="650" cy="34" r="6" fill="#69f0c2"/></svg><div className="absolute bottom-4 left-5 right-5 flex justify-between text-[#8d9188] text-base"><span>Jan</span><span>Mar</span><span>May</span></div></div><div className="rounded-[24px] bg-white/[0.05] border border-white/8 p-5"><h3 className="text-2xl mb-5">Liquidity</h3><div className="grid grid-cols-2 gap-5 items-center"><div className="aspect-square rounded-full bg-[conic-gradient(#31b8d8_0_60%,#a7ff4f_60%_96%,#6ee7b7_96%_100%)] p-8"><div className="h-full w-full rounded-full bg-[#11160f]"/></div><div className="space-y-2">{allocation.map((item) => <div key={item.label} className="rounded-full border border-white/15 bg-black/25 px-3 py-2 font-mono text-sm">{formatCurrency(item.value)}</div>)}</div></div></div><div className="rounded-[24px] bg-white/[0.05] border border-white/8 p-5 flex justify-between"><span className="text-xl">All-time high</span><b className="text-xl">{formatCurrency(totals.netWorth)}</b></div></section>}</div><button onClick={() => router.push('/accounts')} className="fixed right-6 bottom-24 z-40 h-20 w-20 rounded-[24px] border-4 border-[#2f7dff] bg-[#a7ff4f] text-[#071006] shadow-2xl flex items-center justify-center"><Plus className="h-10 w-10"/></button><nav className="fixed left-4 right-4 bottom-5 z-50 mx-auto max-w-[690px] rounded-[30px] border border-white/15 bg-[#10140f]/90 backdrop-blur-xl p-2 shadow-2xl"><div className="grid grid-cols-3 gap-2">{(['accounts','insights','playground'] as Tab[]).map((item) => { const Icon = item === 'accounts' ? Wallet : item === 'insights' ? Waves : Sparkles; return <button key={item} onClick={() => setTab(item)} className={cn('h-14 rounded-[24px] flex items-center justify-center gap-2 font-semibold capitalize text-[#cdd3c8]', tab === item && 'bg-white/18 text-white')}><Icon className="h-5 w-5"/><span className="hidden sm:inline">{item}</span></button> })}</div></nav></div></main>;
}
